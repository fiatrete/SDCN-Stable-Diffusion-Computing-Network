import dbConfig from './dbConfig';
import redisConfig from './redisConfig';
import serverConfig from './serverConfig';
import sdConfig from './sdConfig';

export default { dbConfig, redisConfig, serverConfig, sdConfig };
